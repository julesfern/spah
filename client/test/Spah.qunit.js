$(document).ready(function() {
  
  module("Spah");
  
  test("Locates the state from the data-state attribute on the body element and parses it during initialisation", function() {
    
  });
  
  test("Applies the necessary live bindings to links and forms within the document", function() {});
  
});