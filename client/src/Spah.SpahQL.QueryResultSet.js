/**
 * class Spah.SpahQL.QueryResultSet
 *
 * Wraps an array of QueryResult objects and provides manipulation and traversal methods upon the set.
 **/
Spah.SpahQL.QueryResultSet = function() {};
window["Spah"]["SpahQL"]["QueryResultSet"] = Spah.SpahQL.QueryResultSet;

// Singletons
jQuery.extend(Spah.SpahQL.QueryResultSet, {
  
});

// Instance methods
jQuery.extend(Spah.SpahQL.QueryResultSet.prototype, {
  
});
 